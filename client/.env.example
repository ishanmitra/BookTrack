# Copy to client/.env when the server requires an API key / admin key.
# Exposed to the browser bundle, so use throwaway values in production;
# real protection comes from OAuth + server identity.
VITE_API_KEY=
VITE_ADMIN_TOKEN=